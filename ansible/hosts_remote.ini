[vps]
# L'inventario è minimale. L'IP effettivo viene fornito in modo ad-hoc 
# al momento dell'esecuzione tramite il flag -i <IP>,.
# Usiamo un host placeholder per definire le variabili di base.
temp_target ansible_user=tempuser ansible_python_interpreter=/usr/bin/python3
